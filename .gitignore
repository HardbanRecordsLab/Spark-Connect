# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment variables
.env
.env.local
.env.production

# Supabase CLI local link/session cache (machine-specific, not source)
supabase/.temp

.vercel
.env*

# Test run artifacts — regenerate locally/in CI, not source
playwright-report/
test-results/

# Saved Playwright auth session — contains a real access token, never commit
tests/.auth/
